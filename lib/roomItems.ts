// Single source of truth for prescribed room items by state and room type.
// WA is the only state implemented in MVP.
// NSW, VIC, QLD templates added post-MVP.

export function getPrescribedItems(_state: string, _roomType: string): string[] {
  // Placeholder — will be populated from constants/roomTemplates.ts
  return [];
}
